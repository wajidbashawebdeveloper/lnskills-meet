// next.config.mjs
/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    serverActions: true,  // Adjust based on your needs
  },
};

export default nextConfig;  // Use export default
